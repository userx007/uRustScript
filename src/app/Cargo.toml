[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
interfaces = { path = "../interfaces" }
reader     = { path = "../reader" }
validator  = { path = "../validator" }
parser     = { path = "../parser" }

